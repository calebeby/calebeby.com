<!doctype html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="/main.css" />
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title if title else "Caleb Eby" }}</title>
  </head>
  <body>
    <div class="prose-container">
      {{ content | safe }}
    </div>
  </body>
</html>
